import { SessionSetup } from "@/components/trainer/SessionSetup";

export default function TrainerPage() {
  return (
    <main className="relative mx-auto flex min-h-dvh w-full max-w-[430px] flex-col">
      <SessionSetup />
    </main>
  );
}
